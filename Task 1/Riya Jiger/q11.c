/*
11. Write a C program to rotate the elements of an array by k positions. The user should input the 
size of the array, its elements, the value of k, and the direction of rotation (left or right).
*/
#include <stdio.h>

int main() {
    int n, k, direction;

    
    printf("Enter the number of elements in the array: ");
    scanf("%d", &n);

    
    int arr[n];

    
    printf("Enter the elements of the array:\n");
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    
    printf("Enter the number of positions to rotate: ");
    scanf("%d", &k);

    printf("Enter the direction of rotation (0 for left, 1 for right): ");
    scanf("%d", &direction);

    temp[k];
    for (int i = 0; i < k; i++)


    return 0;
}